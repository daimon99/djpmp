API手册
=========

术语说明
----------

.. glossary::

    包
        英文 `package`，指一个 `python` 目录

    模块
        英文 `module`，指一个具体的 `python` 文件

API手册
----------

.. toctree::
   :maxdepth: 3
   :caption: API手册


.. 可以用 sphinx-apidoc -f -o api/ ../src/ 来生成初始的 api 文档。之后在这里引用
